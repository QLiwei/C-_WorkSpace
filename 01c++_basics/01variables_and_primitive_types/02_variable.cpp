/**
 * @file 01_base_built-in_types.cpp
 * @brief Variable
 * @copyright Copyright (c) 2023
 *
 * Change Logs:
 * Data             Author                          Notes
 * 2023-03-29       vector(vector_qiu@163.com)      first version
 *
 */


/**
 * @brief The application entry point.
 *
 * @return int
 */
int main() {
    /**
     * @brief Variable definition
     *  类型说明符：
     *  初始值：
     *  初始化列表：
     *  默认初始值：
     *      函数体之外的变量初始化为0；
     *      定义在函数体内部的内置类型变量不被初始化
     *  声明(declaration)
     *  定义(definition)
     *
     * 标识符：
     *  变量命名规范：
     *      变量名用小写字母
     *      类名以大写字母开头
     *      标识符有多个单词组成，用_ 连接 或者驼峰命名
     *
     */

    /**
     * @brief 名字作用域
     *
     *  全局作用域(global scope):
     *  块作用域(block scope):
     *
     */

    /**
     * @brief 复合类型（compound type）
     *  引用和指针
     *  引用(reference):为对象起了另外一个名字，引用类型引用另外一种类型
     * ····int ival = 1024;
     * ....int &ref_value = ival; // 引用必须初始化
     *  引用和初始值绑定在一起，不是将初始值拷贝给引用。一旦初始化完成，引用将和它的初始值对象一直绑定在一起。
     *  引用即别名
     *
     * 指针(opinter)：
     * 
     *
     */
    return 0;
}